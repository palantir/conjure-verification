apply plugin: 'com.palantir.conjure'
apply plugin: 'java-library'

dependencies {
    api 'com.palantir.conjure:conjure-core'
    api project("${project.name}-objects")
    api 'com.palantir.conjure.java.runtime:conjure-java-jackson-serialization'
}
